---
interface Props {
  class?: string;
  label?: string;
}

const { class: className = '', label = 'GeoGenesis' } = Astro.props;
---

<span class:list={['site-logo', className]} role="img" aria-label={label}>
  <span class="site-logo-text">{label}</span>
</span>
